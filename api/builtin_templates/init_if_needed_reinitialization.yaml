version: 0.1.3
author: forefy
accent: anchor
name: Init If Needed Reinitialization
description: Anchor accounts declared with init_if_needed can be reinitialized on a subsequent call, so the handler must guard against re-init (for example an is_initialized flag) to prevent an attacker from resetting existing account state.
severity: High
certainty: Medium
rule: |
  # Pass 1 --- collect every state type whose own `init` refuses to run on a
  # live account. `init_if_needed` hands the handler an account that may
  # already exist, so the question is not "can this be called twice" but
  # "does anything stop the second call from overwriting". A guard inside
  # the type's `init` stops it, and that guard is almost never in the file
  # under inspection: the state lives in `state/`, the constraint in
  # `instructions/`. `ast` spans every source in the scan and re-iterates,
  # so collect across all of them first and decide second.
  guarded_types = set()
  for source, nodes in ast:
      try:
          for func in nodes.find_functions_by_names("init", "initialize"):
              # A trait signature has no body, so it can carry no guard.
              if not func.find_by_names("Ok"):
                  continue
              if not func.find_by_names("require", "require_eq", "require_keys_eq"):
                  continue
              if ".impl." not in func.access_path:
                  continue
              # `impl ... for Account<'info, ContractState>` puts two idents
              # under self_ty, and the wrapper is the wrong one: adding
              # "Account" to this set matches every `init_if_needed` field
              # there is and silently disables the rule. Prefer the generic
              # argument, and fall back to the bare ident for an unwrapped
              # `impl ContractState`.
              self_ty_path = func.access_path.split(".impl.")[0] + ".impl.self_ty"
              wrapped = []
              bare = []
              for candidate in nodes.find_by_access_path(".impl.self_ty"):
                  if not candidate.access_path.startswith(self_ty_path):
                      continue
                  if ".arguments.angle_bracketed.args[" in candidate.access_path:
                      wrapped.append(candidate.ident)
                  else:
                      bare.append(candidate.ident)
              for ident in (wrapped if wrapped else bare):
                  guarded_types.add(ident)
      except:
          continue

  for source, nodes in ast:
      try:
          init_nodes = nodes.find_macro_attribute_by_names("init_if_needed").exit_on_none()
          guards = nodes.find_by_names("is_initialized", "initialized")
          contexts = nodes.find_by_names("Context")

          for init_node in init_nodes:
              # The struct carrying init_if_needed, and the handler that takes
              # it. Searching the whole file let a guard belonging to one
              # instruction excuse an unguarded init_if_needed in another.
              struct_path = init_node.access_path.split(".fields")[0]
              if not struct_path.endswith(".struct"):
                  continue

              # A token account is only safe here when its address is pinned
              # too. `associated_token::mint` + `::authority` derives it, and
              # `address = ...` names it, so the create is a no-op on an
              # account the caller cannot choose. Without either, the caller
              # picks the mint and the authority, and it still matters.
              #
              # The trailing "." is load-bearing: `fields.named[1]` is a string
              # prefix of `fields.named[10]`, so matching without a delimiter
              # lets one field's constraints answer for another's.
              field_path = init_node.access_path.split(".attrs")[0] + "."

              def field_has(*idents):
                  for candidate in nodes.find_by_names(*idents):
                      if candidate.access_path.startswith(field_path):
                          return True
                  return False

              if field_has("TokenAccount", "Mint"):
                  if field_has("associated_token") or field_has("address"):
                      continue

              # Pass 2 --- the field's own type guards itself. Reported here
              # this would be a finding whose fix already exists one file over.
              if guarded_types and field_has(*guarded_types):
                  continue

              struct_name = None
              for candidate in nodes.find_by_access_path(struct_path + ".ident"):
                  struct_name = candidate.ident
                  break
              if struct_name is None:
                  for candidate in nodes.find_by_access_path(""):
                      if candidate.access_path == struct_path:
                          struct_name = candidate.ident
                          break

              handler_prefixes = []
              for context in contexts:
                  if ".inputs[" not in context.access_path:
                      continue
                  for arg in context.find_by_access_path(".arguments.angle_bracketed.args[0]"):
                      if struct_name is not None and arg.ident == struct_name:
                          handler_prefixes.append(context.access_path.split(".inputs[")[0])
                      break

              guarded = False
              for guard in guards:
                  if not handler_prefixes:
                      guarded = True
                      break
                  for prefix in handler_prefixes:
                      if guard.access_path.startswith(prefix):
                          guarded = True
                          break
                  if guarded:
                      break
              if guarded:
                  continue

              print(init_node.to_result())
      except:
          continue
